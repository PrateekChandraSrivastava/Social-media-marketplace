// frontend/src/LazyComponent.js
import React from 'react';

const LazyComponent = () => {
    return (
        <div>
            <h2>I am a lazy loaded component!</h2>
        </div>
    );
};

export default LazyComponent;
